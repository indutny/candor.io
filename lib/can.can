// Main binding
log = global.log
compile = global.compile
binding = global.binding

log("Hello world!")
log(global.process.argv)
log(global)

fs = global._bindings.fs

fs.open("/tmp/1.txt", fs.flags.O_RDWR | fs.flags.O_CREAT, 438, (err, fd) {
  log("yay", err, fd)
})

return { yay: true }
